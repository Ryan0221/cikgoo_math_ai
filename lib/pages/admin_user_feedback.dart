import 'package:flutter/material.dart';

class UserFeedbackPanel extends StatelessWidget {
  const UserFeedbackPanel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

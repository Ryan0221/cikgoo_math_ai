import 'package:flutter/material.dart';

class EditContentPanel extends StatelessWidget {
  const EditContentPanel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
